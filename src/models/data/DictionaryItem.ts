/*
import { Instance, SnapshotIn, types } from 'mobx-state-tree';

const ReferenceItemModel = types.model('ReferenceItemModel', {
  id: types.identifier,
  name: types.string,
});

export interface IReferenceItemModel extends Instance<typeof ReferenceItemModel> {}
export interface IReferenceItemModelSnapshotIn extends SnapshotIn<IReferenceItemModel> {}
export default ReferenceItemModel;
*/
